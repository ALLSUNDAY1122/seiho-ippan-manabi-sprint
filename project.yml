name: SeihoIppanManabiSprint
options:
  bundleIdPrefix: com.allsunday1122
  deploymentTarget:
    iOS: "17.0"
targets:
  SeihoIppanManabiSprint:
    type: application
    platform: iOS
    sources:
      - path: native
        excludes:
          - README.md
    resources:
      - path: index.html
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.allsunday1122.seiho.ippan
        INFOPLIST_FILE: native/Info.plist
        MARKETING_VERSION: 1.0.0
        CURRENT_PROJECT_VERSION: 1
        SWIFT_VERSION: 5.0
        TARGETED_DEVICE_FAMILY: "1"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    scheme:
      testTargets: []
